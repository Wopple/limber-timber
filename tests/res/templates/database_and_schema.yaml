version: 1
templates:
# match all QualifiedName.database, and replace their values with "new_project"
- root_type: QualifiedName
  path: database
  value: new_project
# match all QualifiedName.schema_name, and replace their values with "new_dataset"
- root_type: QualifiedName
  path: schema_name
  value: new_dataset
